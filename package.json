{
  "name": "mermaid-forge",
  "version": "1.0.0",
  "description": "Confluence Forge macro for rendering Mermaid diagrams",
  "main": "index.js",
  "scripts": {
    "build": "cd static/mermaid-forge && npm run build && cd ../config && npm run build",
    "lint": "echo 'No linting configured'"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/resolver": "^1.6.0"
  }
}
